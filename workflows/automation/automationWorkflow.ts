import type { WorkflowStep } from '../../agents/types'

export const automationWorkflow: WorkflowStep[] = [
  { step: 'map systems and APIs' },
  { step: 'define trigger and payload contracts' },
  { step: 'build automation pipeline' },
  { step: 'add monitoring and error alerts' },
]
