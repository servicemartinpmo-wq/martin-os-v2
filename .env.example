# Martin OS Command Center (repo root — `pnpm dev` / `npm run dev`)

# Optional: override dev bind/port (defaults: 127.0.0.1, 3000)
# VITE_ROOT_DEV_HOST=127.0.0.1
# VITE_ROOT_PORT=3000

# If you re-introduce Convex in src/router.tsx, set your deployment URL:
# VITE_CONVEX_URL=https://your-deployment.convex.cloud
